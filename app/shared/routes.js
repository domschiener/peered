Router.route('/', {
  name: 'home',
  template: 'home'
});

Router.route('/join', {
  template: 'join'
});

Router.route('/play', {
  template: 'play'
});

Router.route('/game', {
  template: 'game'
});

Router.route('/create', {
  template: 'create'
});
